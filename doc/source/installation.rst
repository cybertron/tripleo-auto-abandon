============
Installation
============

At the command line::

    $ pip install tripleo-auto-abandon

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv tripleo-auto-abandon
    $ pip install tripleo-auto-abandon
